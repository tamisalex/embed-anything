# Minimal dependencies for the Prefect Managed orchestrator flow.
# This installs in Prefect's cloud — no ML libs, no Ray, no torch.
# The ECS task (Dockerfile) carries the heavy deps.
prefect>=3.1
boto3>=1.34
pydantic-settings>=2.3
