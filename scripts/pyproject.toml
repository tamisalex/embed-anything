[project]
name = "embed-anything-scripts"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34",
    "datasets>=2.19",
    "pandas>=2.2",
    "pillow>=10.0",
    "pyarrow>=15.0",
    "tqdm>=4.66",
]

[tool.uv]
dev-dependencies = []
